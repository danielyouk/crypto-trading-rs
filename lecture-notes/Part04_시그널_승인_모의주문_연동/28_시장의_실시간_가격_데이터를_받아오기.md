# 시장의 실시간 가격 데이터를 받아오기

## 이번 클립에서 만들 것

이 클립이 끝나면, 여러분은 한국투자증권 Open API를 통해 **관심 유니버스 종목들의 실시간 현재가, 체결 거래량, 매수/매도 10호가(Order Book) 데이터를 지연 없이 수신**하고, 증권사 API의 초당 호출 제한(Rate Limit / TPS)을 안전하게 관리하는 **'실시간 시세 수집기(`market_data.py`)'**를 완성하게 됩니다.

> 💡 **수강생 안심 안내**: 증권사 HTS를 켜두지 않아도 파이썬 코드가 백그라운드에서 증권사 서버와 통신하여 필요한 종목의 실시간 시세를 0.1초 만에 가져옵니다.

- KIS REST API의 **국내주식 현재가 시세 조회 엔드포인트(`FHKST01010100`)** 구조
- REST API 폴링(Polling) 방식 vs 웹소켓(WebSocket) 스트리밍의 차이점과 장단점
- 증권사 API의 초당 호출 제한(초당 20건 미만)과 **호출 간격 쓰로틀링(Throttling / `time.sleep`) 설계**
- **실습 16**: 4대 전략 유니버스 7종목의 실시간 시세 및 당일 변동폭(Range) 수집 모듈 실행

---

## 이론 핵심

### 1. 실시간 주식 시세 데이터의 구조

KIS 현재가 조회 API를 호출하면 JSON 응답으로 다음 핵심 데이터가 수신됩니다.

```json
{
  "output": {
    "stck_prpr": "35400",     // 주식 현재가
    "prdy_vrss": "450",       // 전일 대비 변동액
    "prdy_ctrt": "1.29",      // 전일 대비 등락률 (%)
    "stck_oprc": "35100",     // 당일 시가 (Open)
    "stck_hgpr": "35650",     // 당일 고가 (High)
    "stck_lwpr": "35050",     // 당일 저가 (Low)
    "acml_vol": "1254300"     // 당일 누적 거래량 (Volume)
  }
}
```

---

### 2. 증권사 API 호출 한도(Rate Limiting)의 법칙

증권사 서버는 특정 프로그램이 1초에 수백 번씩 시세를 요청하면 IP를 차단(HTTP 429 Too Many Requests)합니다.

<div class="compare-box">
  <div class="compare-card bad">
    <div class="tag">❌ 무분별한 무한 루프 호출</div>
    <div class="steps">
      • <code>while True: get_price()</code><br>
      • 1초에 수백 회 호출 발생 ➔ <strong>증권사 서버에서 계좌 일시 차단</strong>.
    </div>
  </div>
  <div class="compare-card good">
    <div class="tag">✅ 쓰로틀링 & 주기적 스케줄링</div>
    <div class="steps">
      • 각 종목 호출 사이 <code>time.sleep(0.1)</code> 적용.<br>
      • 전략 감시 주기를 <strong>1초 간격</strong>으로 규율 있게 폴링 ➔ <strong>안정적 장중 운영</strong>.
    </div>
  </div>
</div>

---

## 실습 — 실습 16: 실시간 시세 수집기 구현

Claude Code 대화창에 아래 프롬프트를 입력하세요.

```prompt
[맥락] 한국투자증권 API를 활용해 전략 대상 종목들의 실시간 시세를 조회하는 시세 수집 모듈을 만들고 싶어. 인증은 클립 27에서 만든 kis_auth.py를 그대로 쓴다.

[만들 것] 다음 조건에 맞춰 파이썬 스크립트 'market_data.py'를 작성하고 실행해줘:
1. .env에 저장된 인증 토큰을 활용해 KIS 국내주식 현재가 시세조회(FHKST01010100) API를 호출하는 함수 'get_current_price(ticker)'를 구현해줘.
2. 4대 전략이 실제로 쓰는 7개 종목의 실시간 시세(현재가, 시가, 고가, 저가, 전일비 등락률, 거래량)를 수집해줘:
   - KODEX 200 (069500) — 추세추종
   - KODEX 레버리지 (122630) — 변동성 돌파
   - TIGER 미국나스닥100 (133690) — 듀얼 모멘텀 공격
   - KODEX 미국채10년선물 (305080) — 듀얼 모멘텀 방어
   - KODEX 단기채권PLUS (214980) — 듀얼 모멘텀 방어
   - KB금융 (105560) — 페어트레이딩
   - 하나금융지주 (086790) — 페어트레이딩
3. 수집한 실시간 시세를 종목별 담당 전략과 함께 깔끔한 표로 출력해줘.
4. API 초당 호출 한도를 준수하기 위해 종목 간 0.1초의 딜레이를 부여해줘. (7종목 한 바퀴에 약 1초)

[하지 말 것]
- kis_auth.py의 인증 코드를 복사해 새로 만들지 말고, 기존 모듈을 불러와서(import) 써. kis_auth.py는 수정하지 마.
- 시세 조회에 실패한 종목의 가격을 지어내거나 전일 종가 등으로 대체하지 마. 실패한 종목은 실패로 표시해.
- API 호출 한도에 걸렸을 때 재시도를 무한히 반복하지 마. 정해진 횟수만 시도하고 멈춰.

[확인] 대화창에 7개 종목의 실시간 시세 표가 조회 시각과 함께 출력되어, 내가 증권사 앱(MTS)의 현재가와 직접 대조할 수 있는지 확인해줘.
```

---

### 내 눈으로 확인할 체크리스트

- [ ] 7개 종목의 현재가, 시가, 고가, 저가, 거래량이 에러 없이 수신되었다. 표의 종목이 Part 2·3에서 백테스트한 종목과 정확히 같은지 대조했다.
- [ ] 출력된 현재가가 실제 증권사 앱(MTS)의 실시간 가격과 정확히 일치한다.
- [ ] API 호출 시 429 에러나 끊김 없이 부드럽게 데이터가 로드되었다.

---

## 다음 클립 예고

실시간 시장 가격 데이터를 내 시스템으로 가져오는 데 성공했습니다!  
다음 클립에서는 이 실시간 가격과 지난 클립들에서 만든 전략 로직을 결합하여, **실시간으로 매수/매도 주문 신호가 발생하는지 판정하는 '실시간 매매 시그널 엔진'**을 구축해 보겠습니다.
