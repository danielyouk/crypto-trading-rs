# DEPRECATED: This file is kept for backward compatibility only.
# The project now uses `pyproject.toml` at the repository root as the single source of truth.
#
# Recommended setup:
#   uv venv --python 3.12 .venv
#   uv pip install -e .
#
# Or with pip:
#   python3.12 -m venv .venv
#   source .venv/bin/activate
#   pip install -e .
#
# ⚠️ IMPORTANT: Python 3.12+ is required for this project.
# Stock pairs reference: reference/python_pairstrading/stock-trading-eda-scheduled_eng.ipynb

# Jupyter / notebooks
jupyter==1.1.1
jupyterlab==4.5.6
ipywidgets==8.1.8
ipython==9.11.0
# Interactive DataTables for DataFrame display in Jupyter
itables==2.7.1

# Core data & visualization
pandas==3.0.1
statsmodels==0.14.6
# Excel .xlsx read/write (pd.read_excel / to_excel)
openpyxl==3.1.5
# PowerPoint .pptx generation
python-pptx==1.0.2
# pd.read_html (e.g. Wikipedia S&P 500 tables) needs an HTML parser
lxml==6.0.2
beautifulsoup4==4.14.3
numpy==2.4.3
plotly==6.6.0
anywidget==0.9.21
matplotlib==3.10.8
mplfinance==0.12.10b0
pillow==12.3.0
imageio==2.37.4
imageio-ffmpeg==0.6.0
koreanize-matplotlib==0.1.1

# Yahoo Finance (stock OHLCV; pulls curl_cffi, beautifulsoup4, peewee, etc.)
yfinance==1.2.0

# Pairs notebook: LinearRegression, Parallel/delayed
scikit-learn==1.8.0
joblib==1.5.3

# Validation (used heavily in pairs_eda.* Pydantic models)
pydantic==2.11.0

# Gemini fallback for S&P 500 (when Wikipedia fails)
google-genai==1.68.0
python-dotenv==1.2.2

# Live monitoring dashboard
streamlit==1.56.0

# Testing
pytest==8.4.1

# Crypto notebooks: REST + WebSocket
requests==2.32.5
websockets==16.0
tqdm==4.67.3

# Lecture notes: MD -> HTML build
Markdown==3.10.3
Jinja2==3.1.6
