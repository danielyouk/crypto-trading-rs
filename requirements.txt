# Python env for notebooks/ (crypto streaming + stock pairs EDA).
# Stock pairs reference: reference/python_pairstrading/stock-trading-eda-scheduled_eng.ipynb
#
# ⚠️ IMPORTANT: Python 3.12+ is required for this project.
# If you  have different python versions such as Python 3.9, you must install Python 3.12 first:
#
# Setup: python3.12 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# S&P 500 helper package (Wikipedia + optional Exa fallback hooks):
#   pip install -e ./python

# Jupyter / notebooks
jupyter==1.1.1
jupyterlab==4.5.6
ipywidgets==8.1.8
ipython==9.11.0
# Interactive DataTables for DataFrame display in Jupyter
itables==2.7.1

# Core data & visualization
pandas==3.0.1
statsmodels==0.14.6
# Excel .xlsx read/write (pd.read_excel / to_excel)
openpyxl==3.1.5
# pd.read_html (e.g. Wikipedia S&P 500 tables) needs an HTML parser
lxml==6.0.2
beautifulsoup4==4.14.3
numpy==2.4.3
plotly==6.6.0
anywidget==0.9.21
matplotlib==3.10.8
mplfinance==0.12.10b0

# Yahoo Finance (stock OHLCV; pulls curl_cffi, beautifulsoup4, peewee, etc.)
yfinance==1.2.0

# Pairs notebook: LinearRegression, Parallel/delayed
scikit-learn==1.8.0
joblib==1.5.3

# Gemini fallback for S&P 500 (when Wikipedia fails)
google-genai==1.68.0
python-dotenv==1.2.2

# Testing
pytest==8.4.1

# Crypto notebooks: REST + WebSocket
requests==2.32.5
websockets==16.0
tqdm==4.67.3
