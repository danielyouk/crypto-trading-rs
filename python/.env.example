# Copy to repo root `.env` (or export in shell). Never commit real values.
# pairs_eda does not read this file by itself — load in YOUR wiring code
# (e.g. google-agents-api-gen) or notebook:  from dotenv import load_dotenv; load_dotenv()

# --- Example keys (adjust names to match google-agents-api-gen / Exa stack) ---
# GOOGLE_API_KEY=
# EXA_API_KEY=
# OPENAI_API_KEY=
